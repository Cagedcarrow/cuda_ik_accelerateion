基于审稿意见，你需要从**文本修改**和**补充实验**两个维度进行完善。以下是具体、可执行的行动清单：

---

## 一、补充实验（必须做，否则评分受限）

### 1. 【核心】混合精度（FP32）加速实验
- **操作**：将雅可比组装、Hessian累加、LM迭代主体迁移至FP32，仅保留线性求解（6x6高斯消元）和最终误差收敛判定在FP64。
- **需报告数据**：
  - 新核函数的**吞吐量**（与纯FP64对比）
  - **寄存器/线程数**、**理论占用率**
  - **Strict SR** 和 **p95误差**（确保精度未退化）
- **预期结果**：即使保守估计，FP32版本吞吐量应提升**2-5倍**。若实测涨幅明显，将此结果作为**新表插入4.1节**，并更新摘要和结论中的核心数据。

### 2. 【基准公平性】cuRobo(num_seeds=16) 对比实验
- **操作**：将cuRobo的种子数从默认1调至16（与本文`K=16`对齐），重新测试`N=100,500,1000`三个规模。
- **需报告数据**：
  - cuRobo(16 seeds)的**吞吐量**、**Strict SR**、**全样本p95**和**成功样本p95**。
- **处理策略**：
  - 若cuRobo(16) SR明显提升（接近或超过本文），则淡化“成功率优势”论述，转而强调本方法在**低批量（N≤200）场景下的调度开销优势**以及**确定性（无随机粒子）收敛行为**。
  - 若cuRobo(16) SR提升不大，则进一步巩固本文在种子利用率上的高效性。

### 3. 【微架构】补充Nsight Compute运行时指标
- **操作**：运行Nsight Compute采集主核函数（`N=1000`）的动态性能计数器。
- **必须报告的关键指标**：
  - **`Warp Stall Long Scoreboard`** 占比（证明FP64管线延迟是主瓶颈）
  - **共享内存实际吞吐量**（验证理论零Bank冲突假设）
  - **`Issue Slot Utilization`**（衡量指令发射效率）
- **位置**：插入4.3节末尾，作为PTX分析的动态数据补充。

---

## 二、文本修改（必须调整，提升严谨性）

### 1. 【关键】迁移混合精度至核心章节
- **原问题**：将混合精度仅作为“未来工作”提及，削弱了本文实用性。
- **修改动作**：
  - 在**第2.3节（无分支LM）**末尾新增一段“混合精度设计讨论”，说明雅可比/Hessian使用FP32、线性求解使用FP64的理论依据（兼顾吞吐与收敛判定精度）。
  - 将**补充实验的FP32结果**整合进**第4.1节（综合性能表5旁边）**，作为本方法的“增强版”性能上限。
  - 结论中明确：“在保持Strict SR≥0.95前提下，混合精度可进一步提升吞吐至XX targets/s（基于实测数据）。”

### 2. 【重要】修正7-DOF可推广性表述
- **原问题**：声称7-DOF仅增加30%计算量（不准确）。
- **修改动作**：
  - 删除具体“30%”数字。
  - 改为：“扩展至Franka Panda 7-DOF时，正规方程维度增至7×7，计算量增量约为6-DOF的1.6倍（理论下限），但本文提出的FusedFK、Target-Block映射和无分支LM迭代框架无需修改，仅需替换线性求解器维度参数——架构可复现性不受影响。”

### 3. 【必要】cuRobo对比段落的重构
- **原问题**：对比不公平，且论述重心偏防御。
- **修改动作**：
  - 新增**4.2节子段落**：“公平种子数下的cuRobo对比”。
  - 基于补充实验（cuRobo K=16）结果，改写结论：“当种子数对齐后，cuRobo在N≥500时吞吐仍占优，但其成功样本p95为0.1-0.3mm（与本方法相当），两者核心差异收敛至调度策略——本方法的单核融合将启动开销压缩至常数，特别适用于低批量、高确定性场景。”

### 4. 【修正】错别字与符号
- Page 9结论第1条：**“又粗”** → **“叉积”**
- Abstract英文：**“analytic gra”** → **“analytic gradients”**
- 表4（Page 6）：修复格式混乱，确保两行对比清晰
- 参考文献[15]：补全MoveIt!文章页码（典型引用为IEEE RAM 2012, pp. 18-19, 但需查标准页码）

---

## 三、Rebuttal（回复信）优先级排序（若需提交）

1. **最优先回应**：混合精度实验数据——这是审稿人最担心的问题（0.33%利用率）。
2. **次优先回应**：cuRobo公平对比——消除“胜之不武”的质疑。
3. **补充说明**：7-DOF计算量修正，承认原文表述不严谨并给出修正数据。

---

## 四、工作量评估与Deadline建议

| 项目 | 预估工作量 | 优先级 |
|------|------------|--------|
| FP32混合精度实验与数据整理 | 1-2天（需重新编译运行） | **致命优先级** |
| cuRobo(seed=16)补测 | 半天（修改参数重跑） | 高 |
| Nsight Compute数据采集 | 半天（需环境支持） | 中 |
| 文本修改（上述4处） | 2-3小时 | 高 |

**建议顺序**：先跑FP32实验 → 同时跑cuRobo补测 → 采集Nsight数据 → 修改文本。

完成上述修改后，论文评分可望从**8.2提升至9.0+**（接收概率大幅提高）。若时间紧张，**至少完成FP32实验和cuRobo补测**，这两项是审稿人的核心关切点。